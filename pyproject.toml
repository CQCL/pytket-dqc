[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pytket-dqc"
version = "0.0.1"
requires-python = ">= 3.8"
dependencies = [
    'numpy == 1.11',
]